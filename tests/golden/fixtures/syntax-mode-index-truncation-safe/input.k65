func main @a8 @i8 {
    @i16
    ldx #$1234
    ldy #$5678
    @i8
    ldx #$34
    ldy #$78
    // `pha` between transfers keeps both observable so the optimizer
    // does not fold the dead `txa` into the following `tya`.
    txa
    pha
    tya
}
